{
    "compilerOptions": {
        // Don't emit files because the app-server get's transpiled by webpack
        "noEmit": true,
        "outDir": "app-server/build",
    },
    "extends": "../../tsconfig.json"
}